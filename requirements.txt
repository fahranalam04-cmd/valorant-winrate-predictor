# Version floors, not pins. Phase 0 verifies these resolve on the local Python
# and falls back to Python 3.12 if ML wheels are not yet published for 3.14.

# HTTP + config
httpx>=0.27
python-dotenv>=1.0
tenacity>=8.2          # retry/backoff around 429s

# Data
pandas>=2.2
numpy>=1.26
pyarrow>=16.0          # parquet for feature matrices

# Modelling
scikit-learn>=1.5
lightgbm>=4.3
xgboost>=2.1
shap>=0.45
joblib>=1.4            # model persistence; used directly, not via sklearn
                       # NOTE: joblib's pickle loader uses a NumPy API
                       # deprecated in 2.5. The live path loads this
                       # bundle, so raise the floor when upstream fixes it.
matplotlib>=3.9        # reliability diagrams, feature importance plots

# Live client + dashboard
websockets>=12.0
fastapi>=0.110
uvicorn[standard]>=0.29
psutil>=5.9            # locate the Riot client process

# Coaching layer (Phase 8)
anthropic>=0.40

# Dev
pytest>=8.0
ruff>=0.4
